# ─── Web ───
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── Database ───
# psycopg[binary]: psycopg2 후속. Neon/Supabase 풀러와 호환
psycopg[binary]==3.2.3
SQLAlchemy==2.0.36

# ─── HTTP / 재시도 ───
# 런타임 재시도는 tenacity의 stop_after_delay 사용.
# stop_after_attempt만 쓰면 총 소요 시간이 보장되지 않는다.
httpx==0.28.1
tenacity==9.0.0

# ─── RAG / 문서 처리 ───
pypdf==5.1.0

# ─── 데이터 생성 ───
Faker==33.1.0

# ─── LLM ───
# Gemini 확정. 근거는 docs/decisions/002 참조.
# 응답 스키마 지정으로 JSON 출력을 강제할 수 있다는 점이 결정적이었다.
# ParsedQuery.category와 paymentType은 값 집합이 고정이므로
# 모델이 임의 문자열을 낼 여지를 스키마 단계에서 없앤다.
google-genai==1.2.0

# ─── Dev ───
pytest==8.3.4
pytest-asyncio==0.25.0
