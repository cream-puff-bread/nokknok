# ─── Web ───
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── Database ───
# psycopg[binary]: psycopg2 후속. Neon/Supabase 풀러와 호환
psycopg[binary]==3.2.3
SQLAlchemy==2.0.36
pgvector==0.3.6

# ─── HTTP / 재시도 ───
# 런타임 재시도는 tenacity의 stop_after_delay 사용.
# stop_after_attempt만 쓰면 총 소요 시간이 보장되지 않는다.
httpx==0.28.1
tenacity==9.0.0

# ─── RAG / 문서 처리 ───
pypdf==5.1.0

# ─── 데이터 생성 ───
Faker==33.1.0

# ─── LLM SDK ───
# ⚠️ Day 1에 제공자를 확정한 뒤 아래 중 하나를 활성화할 것.
#    docs/decisions/001-llm-embedding-provider.md 참조.
#
#    규칙 변환·설명 생성과 임베딩은 별개로 정해야 한다.
#    Anthropic API에는 임베딩 엔드포인트가 없으므로,
#    Claude를 쓰더라도 임베딩은 다른 제공자가 필요하다.
#
#    임베딩 모델을 바꾸면 schema.sql의 vector(1536)도 함께 수정해야 한다.
#    (text-embedding-3-small = 1536 기준)
#
# anthropic==0.42.0
# openai==1.59.0
#
# SDK 없이 httpx로 직접 REST 호출하는 것도 가능하다.
# 그 경우 위 두 줄을 활성화하지 않고 이 주석만 남긴다.

# ─── Dev ───
pytest==8.3.4
pytest-asyncio==0.25.0
