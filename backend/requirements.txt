# ─── Web ───
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── Database ───
# psycopg[binary]: psycopg2 후속. Neon/Supabase 풀러와 호환
psycopg[binary]==3.2.3
SQLAlchemy==2.0.36

# ─── HTTP / 재시도 ───
# 런타임 재시도는 tenacity의 stop_after_delay 사용.
# stop_after_attempt만 쓰면 총 소요 시간이 보장되지 않는다.
httpx==0.28.1
tenacity==9.0.0

# ─── RAG / 문서 처리 ───
pypdf==5.1.0

# ─── 데이터 생성 ───
Faker==33.1.0

# ─── LLM SDK ───
# ⚠️ Day 1에 제공자를 확정한 뒤 아래 중 하나를 활성화할 것.
#
#    임베딩은 사용하지 않기로 했으므로(docs/decisions/001) 규칙 변환과
#    결과 설명에 쓸 모델 하나만 정하면 된다.
#    선택 시 고려할 점은 docs/decisions/002 참조.
#
# anthropic==0.42.0
# openai==1.59.0
#
# SDK 없이 httpx로 직접 REST 호출하는 것도 가능하다.
# 그 경우 위 두 줄을 활성화하지 않고 이 주석만 남긴다.

# ─── Dev ───
pytest==8.3.4
pytest-asyncio==0.25.0
