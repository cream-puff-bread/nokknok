# 002. LLM 제공자 및 배치 파이프라인 구조

상태: **미확정 — Day 1 결정 필요**
관련: `backend/requirements.txt`, `contracts/schema.sql`, `docs/decisions/001`

001에서 벡터 검색을 걷어내면서 두 가지가 열린 채로 남았다.
확정 문서 안에 미결을 두면 추적이 어려우므로 여기로 분리한다.

---

## 결정 1. LLM 제공자

### 배경

`.env.example` 에 `LLM_API_KEY` 와 `LLM_MODEL` 이 있고 `httpx` 와 `tenacity` 도 준비되어
있으나, 제공자를 확정하지 않아 `requirements.txt` 의 SDK가 주석 처리된 상태다.

임베딩을 쓰지 않기로 했으므로(001) **규칙 변환과 결과 설명에 쓸 모델 하나만** 정하면 된다.

### 고려할 점

| 항목 | 내용 |
|---|---|
| 사용처 | 약관 → 조건식 변환(배치), 질의 파라미터 추출(런타임), 결과 설명 생성(런타임) |
| 정확성 요구 | 배치는 사람 검수 게이트가 있어 완벽할 필요 없음. 런타임 파라미터 추출은 실패 시 422 응답 |
| 응답 속도 | 런타임 설명 생성은 총 3.5초 예산. 모델 지연이 예산을 넘기면 항상 null이 된다 |
| 비용 | 유일한 유료 항목. 약관 변환 결과를 파일로 캐싱해 반복 호출을 막는다 |
| Rate limit | 계정 등급이 낮으면 분당 요청 수 제한이 빡빡하다. 배치에서 429가 나기 쉽다 |

### 결정 시 함께 확인할 것

- 선택한 모델의 평균 응답 시간이 런타임 예산(3.5초) 안에 들어오는가
- 배치 처리 시 분당 요청 한도가 약관 변환 물량을 감당하는가
- SDK를 쓸지 `httpx` 로 직접 REST 호출할지

### 결정 사항 (회의 후 기입)

- 제공자 및 모델:
- SDK 사용 여부:
- 결정 근거:

---

## 결정 2. 배치 파이프라인 구조

### 배경

약관에서 규칙을 뽑고 근거 조항을 연결하는 방식이 두 가지다.

**1단계 구조** — 조항을 순차 처리하며 각 조항에서 그 자리에서 규칙을 추출한다.
추출 시점에 `clause_source.id` 를 이미 알고 있으므로 그대로 함께 저장한다.
조항 검색이 아예 필요 없다.

**2단계 구조** — 조항을 먼저 전부 적재하고, 이후 규칙과 조항을 따로 매칭한다.
매칭 단계에서 검색이 필요하며, `pg_trgm` 유사도를 쓴다.

### 왜 정해야 하는가

현재 스키마는 결정 전이라 `pg_trgm` 확장과 `idx_clause_content_trgm` GIN 인덱스를
생성해 둔 상태다. 수백 row 규모에서 비용은 무시할 수준이지만, 1단계로 확정되면
불필요한 구조물이 남는다.

### 확정 후 조치

| 결정 | 조치 |
|---|---|
| 1단계 | `contracts/schema.sql` 에서 `CREATE EXTENSION pg_trgm` 과 `idx_clause_content_trgm` 제거 |
| 2단계 | 현행 유지. `similarity()` 조회 쿼리를 `src/rag/` 에 구현 |

### 결정 사항 (회의 후 기입)

- 파이프라인 구조:
- 스키마 정리 여부:
- 결정 근거:
