# 협업 규칙

## 계약 파일

`contracts/` 의 네 파일은 세 사람의 작업이 만나는 지점이다.

| 파일 | 작성 | 내용 |
|---|---|---|
| `schema.sql` | @seohee-P | 테이블 정의 |
| `api-spec.yaml` | @fanfanduck | 엔드포인트와 응답 형식 |
| `types.ts` | @fanfanduck | 프론트 데이터 타입 |
| `ui-system.md` | 전원 합의 | 디자인 토큰과 클래스 조합 |

**변경은 전원 합의 후에만 한다.** 테이블 구조가 바뀌면 @mango606의 적재 로직과 @seohee-P의 판정 로직이 동시에 깨진다. 변경 시 마이그레이션 파일과 계약 파일을 같은 커밋에 포함시킨다.

## AI 보조 코딩 규칙

### 계약 파일을 컨텍스트로 제공한다

AI에게 코드를 요청할 때 관련 계약 파일 내용을 함께 넣고 "이 스키마와 타입을 그대로 사용하라"고 명시한다. 이 규칙을 지키지 않으면 각자 다른 필드명을 쓰게 되고, 통합에 하루 이상 소모된다.

`.claude/skills/` 에 스킬을 두었으므로 Claude Code, Cursor 등을 쓰는 경우 자동으로 참조된다.

### 해피 패스만 짜지 않게 한다

AI는 정상 동작하는 경로만 작성하려는 경향이 있다. 백엔드 코드 생성 시 다음을 프롬프트에 포함한다.

> 모든 외부 API 호출(LLM 등)과 데이터베이스 조회에 예외 처리를 포함하고, 실패 시 에러 로깅과 대체 응답 경로를 함께 작성할 것. 타임아웃을 명시적으로 설정할 것. 외부 API 호출에는 지수 백오프 재시도를 적용하되, 429 및 5xx 응답 시 대기 시간을 2배씩 늘리며 재시도하고 Retry-After 헤더가 있으면 그 값을 우선 따를 것.

### 생성된 코드는 읽고 커밋한다

최소한 아래는 직접 확인한다.

- 계약 파일의 필드명과 일치하는가
- 예외 처리가 있는가
- 하드코딩된 값이 남아 있지 않은가

면접에서 "이 코드 왜 이렇게 짰나요"에 답할 수 없는 코드는 포트폴리오가 되지 않는다.

## 응답 조립 책임

세 사람의 결과물이 한 응답에 합쳐지는 지점이 있다. **엔진은 계산만, RAG는 검색만, API 계층(fanfanduck)이 조립한다.**

`/api/route` 처리 순서:

| 단계 | 담당 | 산출물 | 실패 시 |
|---|---|---|---|
| 1. 최적 조합 계산 | @seohee-P | `RouteCandidate[]` | 오류 응답 |
| 2. 근거 조항 조회 | @fanfanduck (스키마는 @mango606) | `ClauseRef[]` | 빈 배열 |
| 3. 설명 생성 | @fanfanduck (LLM) | `string` | `null` |
| 4. 조립 | @fanfanduck | `RouteOption` | — |

2·3단계가 실패해도 1단계 결과는 그대로 응답에 실린다. **설명만 빠지고 숫자는 나오는 것이 정상 동작이다.**

### 엔진은 설명의 존재를 모른다

`RouteCandidate`에는 `explanation`과 `clauses`가 없다. 엔진이 LLM에 의존하지 않아야 단위 테스트가 목 없이 돌아가고, 계산과 생성을 분리한 아키텍처 원칙이 유지된다.

### 근거 조항은 조인으로 가져온다

`card_benefit_rule`이 `clause_id`를 FK로 갖고 있고, 엔진은 자신이 적용한 규칙을 안다. 그 `ruleId`를 결과에 실으면 정확한 조인 한 번으로 근거를 특정할 수 있다.

```sql
SELECT cs.content, cs.doc_name, cs.page_no
FROM card_benefit_rule r
JOIN clause_source cs ON cs.id = r.clause_id
WHERE r.id = $1;
```

근거 조항 조회가 결과를 반환하려면 `card_benefit_rule.clause_id` 가 채워져 있어야 한다.
규칙을 적재할 때 반드시 근거 조항을 함께 넣는다. 시드 데이터는 `data/clauses.seed.sql`
에서 이를 처리한다.

**런타임 경로에서는 어떤 형태의 검색도 사용하지 않는다.** 판정 근거를 추측으로 찾으면 실제 적용된 규칙과 어긋날 수 있다. 조항 검색은 @mango606의 배치 파이프라인이 2단계로 동작하는 경우에만 필요하며, 그때도 벡터가 아닌 `pg_trgm` 유사도를 쓴다. 근거는 `docs/decisions/001` 참조.

### 설명은 best에만, 타임아웃 예산 안에서

`alternatives`는 `RouteCandidate[]`이므로 `explanation` 필드 자체가 없다. 항상 `null`인 필드를 남기지 않기 위해서다.

`/api/route`는 동기 엔드포인트이므로 설명 생성 전체에 **총 3~4초 예산**을 건다. 재시도 횟수가 아니라 총 소요 시간이 기준이며, 예산을 넘기면 재시도 중이라도 포기하고 `null`로 응답한다.

## 규칙 적용 우선순위

같은 실적 구간에 카테고리 전용 규칙과 `ALL` 와일드카드 규칙이 함께 있을 수 있다.
**적용 규칙은 하나만 선택하며 합산하지 않는다.**

1. 결제 카테고리와 정확히 일치하는 규칙이 있으면 그것을 적용
2. 없으면 `ALL` 규칙을 폴백으로 적용
3. 둘 다 없으면 할인 없음

카드 C는 ONLINE 10%와 ALL 1%를 함께 갖지만, ONLINE 결제의 할인율은 **11%가 아니라 10%**다.
합산하면 실제로 존재하지 않는 할인이 화면에 표시된다. 구현 세부는 `backend/README.md` 참조.

## 재시도 정책

배치와 런타임의 대응을 다르게 하며, 설정값도 분리한다.

| 구분 | 환경변수 | 값 | 실패 시 |
|---|---|---|---|
| 배치 (약관 변환) | `LLM_BATCH_MAX_RETRY` / `LLM_BATCH_TIMEOUT_MS` | 5회 / 15초 | 처리 완료 목록을 파일로 남겨 중단 지점부터 재개 |
| 런타임 (질의·설명) | `LLM_RUNTIME_TIMEOUT_BUDGET_MS` | 총 3.5초 | 대체 응답 즉시 반환 |

런타임에는 재시도 횟수 설정이 없다. `stop_after_delay` 는 예산만 보므로 횟수 값을
두면 실제 효과는 없는데 바꾸면 동작이 달라진다고 오해하게 된다.

**두 경로가 같은 설정값을 공유하면 안 된다.** 런타임에서 배치용 값(5회, 15초)을 쓰면 시연 중 화면이 1분 가까이 멈춘다.

런타임은 재시도 횟수가 아니라 **총 소요 시간 예산**이 응답 시간을 결정한다. 예산을 넘기면 재시도 도중이라도 중단하고 `explanation=null`로 응답한다.

## 데이터베이스 연결

**반드시 풀링(Pooled) 주소를 사용한다.** 무료 티어는 동시 연결 한도가 낮아 Direct 주소로는 금방 초과된다.

- 애플리케이션 풀 크기는 5 내외로 제한
- 배치 스크립트는 작업 종료 시 연결을 명시적으로 닫는다
- Render 인스턴스가 슬립에서 깨어날 때 커넥션이 새로 맺어지므로, 정리되지 않은 연결이 누적되지 않는지 확인한다

## 브랜치와 커밋

```
main              항상 배포 가능한 상태
feat/<domain>     기능 개발
fix/<domain>      버그 수정
refactor/<domain> 리팩터링
```

커밋 메시지는 Conventional Commits 형식을 따른다.

```
feat: 결제 조합 최적화 엔진 가지치기 로직 추가
fix: 전월실적 산정 시 무이자 할부 제외 누락 수정
refactor: 거래 데이터 조회를 인터페이스로 분리
test: 실적 판정 로직 단위 테스트 추가
docs: 약관 변환 파이프라인 설명 보강
```

**하루 한 번은 반드시 병합한다.** 며칠씩 쌓아두고 한꺼번에 합치면 충돌이 감당하기 어려워진다.

## CI

PR 마다 `.github/workflows/ci.yml` 이 백엔드 테스트와 프론트 빌드를 돌린다.

| 잡 | 하는 일 |
|---|---|
| `backend` | `pytest -q` (통합 테스트 포함) |
| `frontend` | `npm ci` → `npm run typecheck` → `npm run build` |

**PR 본문에 테스트 수를 적을 때는 CI 결과를 기준으로 한다.** 로컬 수치는 환경에
따라 달라진다 — 실제로 며칠 사이 세 번 어긋났고 전부 환경 차이였다(리베이스한
로컬 vs 푸시된 구버전, `.env` 의 LLM 키 유무, `DATABASE_URL` 유무). 매번 사람이
손으로 재현해 원인을 찾았다.

통합 테스트는 저장소 시크릿 `DATABASE_URL` 로 실제 DB에 붙는다. `conftest` 가
트랜잭션을 열고 무조건 롤백하므로 시드 데이터는 건드리지 않는다. **시크릿이
없으면 통합 테스트만 자동으로 skip 되고 단위 테스트는 그대로 돈다** — 즉 시크릿을
빠뜨려도 CI 가 초록불로 뜨므로, 통합 테스트가 실제로 돌았는지는 skip 수로
확인한다.

```
DATABASE_URL 있음  →  205 passed
DATABASE_URL 없음  →  162 passed, 43 skipped
```

Node 버전은 `frontend/.nvmrc` 를 단일 출처로 삼는다. 워크플로에 버전을 다시 적으면
로컬과 CI 가 조용히 갈라진다.

## 코드 리뷰

각자 담당 도메인이 달라 깊은 리뷰는 현실적이지 않다. **인터페이스가 바뀌는 PR은 반드시 다른 두 명이 확인한다.** 내부 구현은 신뢰하고 맡긴다.

## 보안

- API 키와 DB 접속 정보는 환경변수로 관리하고 코드에 직접 쓰지 않는다
- 업로드 파일은 확장자와 크기를 검증한다
- 외부 URL을 받아 요청하는 코드는 작성하지 않는다
- 개인 금융정보는 로그에 남기지 않는다

## 배포

모노레포이지만 프론트와 백엔드는 다른 플랫폼에 배포한다.

| 플랫폼 | Root Directory | 빌드 감시 경로 |
|---|---|---|
| Vercel | `frontend` | Ignored Build Step으로 `frontend/` 변경만 빌드 |
| Render | `backend` | Build Filters로 `backend/**` 지정 |

설정하지 않으면 한쪽만 고쳐도 양쪽이 모두 재배포되어 빌드 시간이 낭비되고, 재배포 중 서비스가 잠시 끊긴다.

플랫폼 설정은 `render.yaml` 과 `frontend/vercel.json` 에 정의돼 있다. 대시보드에서만
되는 설정, 비밀 환경변수 목록, 배포 후 확인 절차는 **[docs/deployment.md](./docs/deployment.md)** 참조.

화면 코드는 배포 환경에서도 `/api/...` 상대경로로만 호출한다. Vercel rewrite 가 백엔드로
전달하므로 브라우저 CORS 가 걸리지 않고, 화면 코드가 로컬과 배포에서 완전히 같다.

## 시연 안정성

- 규칙 테이블 DB 스냅샷을 미리 백업하고 시연에는 이 스냅샷을 사용한다
- LLM 호출 구간에는 타임아웃과 대체 응답을 준비한다
- **시연 5분 전에 워밍업한다.** 콜드스타트가 두 겹으로 겹쳐 있다.

  ```bash
  curl -s https://nokknok.vercel.app/api/health    # Render 깨우기
  curl -s https://nokknok.vercel.app/api/personas  # Neon 깨우기
  ```

  `/api/personas` 가 핵심이다. DB까지 건드려야 Neon 컴퓨트가 깬다.

  2026-08-26 실측(43분 방치 후 첫 요청):

  | 구간 | 시간 |
  |---|---|
  | 화면 정적 자산 (Vercel CDN) | 0.05초 |
  | 첫 API 요청 — Render 슬립 웨이크 | **32.6초** |
  | 첫 데이터 요청 — Neon 웨이크 | **+3.1초** |
  | 이후 모든 요청 | 0.17초 |

  **화면은 즉시 뜨는데 데이터가 36초 안 온다.** 정적 자산은 Vercel CDN에서
  나오고 API만 Render를 타기 때문이다. 심사위원 눈에는 "화면은 떴는데 계속
  로딩만 도는" 상태로 보인다.

  잠드는 조건이 다르다.

  | | 조건 | keep-alive 로 막히나 |
  |---|---|---|
  | Render 슬립 | 15분 무응답 | ✗ 실제 실행 간격이 55분까지 벌어진다 |
  | Neon 자동 정지 | 5분 무응답 | ✗ `/api/health` 가 DB를 안 건드린다 |

  Vercel rewrite 는 Render 웨이크를 끝까지 기다린다(32.6초 대기 후 200).
  프록시 타임아웃으로 끊길 것을 우려했으나 실측에서 발생하지 않았다.
- LLM 호출을 의도적으로 실패시켜 화면이 어떻게 보이는지 확인한다
- 네트워크 문제에 대비해 화면 녹화본을 확보한다

## 기록

개발 중 겪은 문제와 해결 과정을 `docs/decisions/` 에 남긴다.

- 어떤 문제를 만났는지
- 어떤 선택지를 고려했고 왜 그것을 택했는지
- 결과가 수치로 어떻게 달라졌는지

최적화 전후 실행 시간, 규칙 변환 정확도, API 응답 시간은 그대로 면접 재료가 된다. 끝난 뒤 되짚으면 기억이 흐려지므로 그때그때 남긴다.
